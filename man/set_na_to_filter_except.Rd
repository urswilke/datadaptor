% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/command_block_functions.R,
%   R/command_block_vector_functions.R
\name{set_na_to_filter_except}
\alias{set_na_to_filter_except}
\title{Recode missing values of variables in dataframe}
\usage{
set_na_to_filter_except(df, recode_na_exceptions, replace_val, replace_label)

set_na_to_filter_except(df, recode_na_exceptions, replace_val, replace_label)
}
\arguments{
\item{df}{dataframe}

\item{recode_na_exceptions}{character vector of variables to exclude from recoding}

\item{replace_val}{value to replace missing values by}

\item{replace_label}{value label of replacing value}
}
\value{
Dataframe where \code{set_na_to_filter()} is run on all numeric variables
(except those in \code{recode_na_exceptions})
\code{replace_val} and \code{replace_label} are the arguments passed to \code{set_na_to_filter()}.

Dataframe where \code{set_na_to_filter()} is run on all numeric variables
(except those in \code{recode_na_exceptions})
\code{replace_val} and \code{replace_label} are the arguments passed to \code{set_na_to_filter()}.
}
\description{
Recode missing values of variables in dataframe

Recode missing values of variables in dataframe
}
\examples{
spss_file <- system.file("extdata", "fake_survey.sav", package = "datenanpassr")
df <- haven::read_sav(spss_file)
set_na_to_filter_except(
  df,
  c("q1", "q2"),
  -2,
  "I'm the label for the missing value replacement"
)
spss_file <- system.file("extdata", "fake_survey.sav", package = "datenanpassr")
df <- haven::read_sav(spss_file)
set_na_to_filter_except(
  df,
  c("q1", "q2"),
  -2,
  "I'm the label for the missing value replacement"
)
}
