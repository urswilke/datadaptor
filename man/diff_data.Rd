% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_data_table.R
\name{diff_data}
\alias{diff_data}
\title{Diff to labelled data frames}
\usage{
diff_data(df1, df2, id_var = "DC_ID", n_max = 20, warn = TRUE)
}
\arguments{
\item{df1}{data frame 1}

\item{df2}{data frame 2}

\item{id_var}{name of the id variable (string)}

\item{n_max}{Maximum number of values/value labels in variables. Variables
containing more than \code{n_max} won't be printed.}

\item{warn}{whether to emit a warning if \code{df1} and \code{df2} don't contain the
same ids.}
}
\value{
data frame of diff results:
For every variable \code{var}in the data.frames, the counts \code{n} are shown for
all the values (one column per value type), variable and value labels, well
as their type (column prefixes). The column suffixes \code{"_old"} and \code{"new"}
indicate \code{df} and \code{df2}, respectively. If the type column is empty, the
variable doesn't exist in the respective data.frame.
}
\description{
Diff to labelled data frames
}
\examples{
mapping_file <- system.file("extdata", "mapping.xlsx", package = "datenanpassr")
mapping <- Mapping$new(mtcars_labelled, mapping_file)
mapping$modify_data()
diff_data(mapping$dat, mapping$dat_mod, "id")
}
