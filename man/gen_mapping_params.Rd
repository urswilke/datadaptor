% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{gen_mapping_params}
\alias{gen_mapping_params}
\title{Mapping parameters}
\usage{
gen_mapping_params(
  mapping_file = NULL,
  mapping_type = "excel",
  excel_params = extract_named_region_params(mapping_file, wb),
  id_var = NULL,
  error_out = "unsafe",
  debug = FALSE,
  save_path = tempdir(),
  write_mapping_to_txt = FALSE,
  expr_eval_env = new.env(parent = baseenv()),
  lab_before_var_sheet = "yes",
  miss_rec_lab = "FILTER",
  miss_rec_val = -2,
  na_to_filter = TRUE,
  not_miss_to_filter_vars = NA_character_,
  lowercase_varnames = FALSE,
  database_dsn = NULL,
  qrow_db_write = FALSE,
  wb,
  ...
)
}
\arguments{
\item{mapping_file}{Path of the Excel mapping file. Alternatively, you can
pass an R list object containing named dataframes that is in the shape of
the \code{Mapping$cmd$sheet_data_raw} field (see section
"Parse the sheets to R list objects" in
\code{vignette("translating_command_blocks_to_R")}).}

\item{mapping_type}{String specifying the mapping type.
Either "excel" or "list". Defaults to "excel".}

\item{excel_params}{Params parameters read from Excel file; see
\code{extract_named_region_params()}.}

\item{id_var}{character string of the id variable name in the dataset.}

\item{error_out}{character string. Either "safe" or "unsafe" (the default).
Whether to continue executing when a command block fails, or to error out.
Adds a column "error" to the mapping's command table \code{mapping$cmd_tbl}.}

\item{debug}{whether to enter in debug mode when an error occurs.
Automatically sets \code{error_out = "safe"}.}

\item{save_path}{filepath where to save files.}

\item{write_mapping_to_txt}{Whether to write the \code{Mapping}'s data to text
files (for instance, in order to allow for git version control during the
course of a project that evolves). Defaults to FALSE. Will probably be
deprecated in the future.}

\item{expr_eval_env}{The environment where expressions are evaluated. See
\code{?safer_env}.}

\item{lab_before_var_sheet}{Whether to apply the "Label" sheet before the
"Variables" sheet. Defaults to \code{TRUE}.}

\item{miss_rec_lab}{Label given if \code{na_to_filter = TRUE}.}

\item{miss_rec_val}{Replace value if \code{na_to_filter = TRUE}.}

\item{na_to_filter}{if TRUE (the default), NA values ("missing" in SPSS) are
transformed with. \code{apply_command.cmd_recna_xcpt()} in the first command
block.}

\item{not_miss_to_filter_vars}{Space separated character string of variable
names spared out for \code{apply_command.cmd_recna_xcpt()}.}

\item{lowercase_varnames}{Whether to transform all variable names to
lowercase during data modification, and rename them back to their original
case (if still existing) in the end.}

\item{database_dsn}{Defaults to \code{NULL}; Character string of the database dsn.
Only used in crosstabser.}

\item{qrow_db_write}{Defaults to \code{FALSE};
Should the crosstabs data be written to the database in the calculation of every Qrow?}

\item{wb}{For an excel mapping, the openxlsx2 workbook object, otherwise \code{NULL}.}

\item{...}{used to pass arguments from \code{Mapping$new(...)}}
}
\value{
list object (see examples)
}
\description{
\code{gen_mapping_params()} is a helper function to generate the
parameters in the \code{params} field when a Mapping object is constructed with
\code{Mapping$new()}. It generates a list of named elements with mapping
parameters. The argument values are the below default values, then
overwritten if passed by the \code{...} dots, and then overwritten by the Excel
file.
}
\examples{
# Only for documentation purposes:
# (`gen_mapping_params()` isn't supposed to be be called directly).
mapping_file <- system.file(
  "extdata",
  "mapping.xlsx",
  package = "datenanpassr"
)
class(mapping_file) <- "excel"
wb <- openxlsx2::wb_load(mapping_file)

gen_mapping_params(mapping_file, wb = wb)
}
