% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{save_mapping}
\alias{save_mapping}
\title{Save the modified data of a mapping to a file}
\usage{
save_mapping(
  mapping,
  path = NULL,
  show = FALSE,
  name = "dat",
  filetype = "sav",
  ...
)
}
\arguments{
\item{mapping}{\code{Mappjng} object}

\item{path}{\code{character()} vector or \code{NULL}. If \code{NULL} (the default) it
will write the file to the path in \code{self$params$save_path} with
the file \code{name}(s) & \code{filetype}(s) specified.}

\item{show}{Whether to directly open the file (needs the according
software installed and setup to open its filetype).}

\item{name}{\code{character()} vector containing all the filenames to be
written. Needs to be of length 1 or the same length as \code{filetype}. Is
overwritten, by \code{path} if not \code{NULL}.}

\item{filetype}{\code{character()} vector containing all the filetypes to be
written. Is overwritten, by \code{path} if not \code{NULL}.}

\item{...}{used to pass arguments from \code{Mapping$save(...)}}
}
\description{
The data can be exported to the file formats of Stata & SPSS. The Excel
export removes variable & value labels. Rmarkdown filetypes ("Rmd") can
be used to setup a python session with the data. Also see
\code{vignette("debugging")}.
}
\examples{
\dontrun{
# Create a Mapping object from the files provided by the package:
mapping_file <- system.file("extdata", "mapping.xlsx", package = "datenanpassr")
spss_file <- system.file("extdata", "fake_survey.sav", package = "datenanpassr")
m <- Mapping$new(spss_file, mapping_file)

# The method applies the modifications specified in a command_blocks object
m$modify_data(command_blocks = m$cmd_tbl$command_blocks)
m$save("stata_data.dta", show = TRUE)
}
}
