% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmd_table.R
\name{new_mapping}
\alias{new_mapping}
\title{Constructor of mapping objects}
\usage{
new_mapping(
  mapping_file,
  na_to_filter = TRUE,
  vectorized = FALSE,
  df_cmd = tibble::tibble(),
  data = tibble::tibble(),
  try_catch = FALSE,
  add_r_command_colum = FALSE,
  rec_fun = purrr::reduce2,
  check_id_is_unique = TRUE,
  mapping_file_attrs = list(),
  class = character()
)
}
\arguments{
\item{mapping_file}{filename of the Excel mapping file}

\item{na_to_filter}{logical specifying whether a command is added whether
\code{set_na_to_filter_except()} should be run as the very first command.}

\item{vectorized}{logical whether groups of command blocks to calculate
new vectors are applied to the data in a single \code{dplyr::mutate()}
statement or whether to consecutively apply (by using \code{purrr::reduce()})
each command expression on the whole data frame. Probably something similar as the difference between:
dataframe() \%>\% mutate(a = 1) \%>\% mutate(b = 2) or
dataframe() \%>\% mutate(a = 1, b = 2).
The second is faster. For many data operations or large datasets,
vectorized = TRUE should also be faster}

\item{df_cmd}{Table of commands (generated with \code{mapp_cmd_table()}); defaults to empty data.frame.}

\item{data}{Dataset that is modified with the commands of \code{df_cmd}.}

\item{try_catch}{logical; if TRUE, command blocks of the mapping file
that error out will be skipped; possible errors are attached to the
dataframe as a character vector of length of all the commands in the
command table; in combination with \code{rec_fun} = \code{purrr::accumulate2} this
can be used to examine intermediate results, in order to find the reason
for the error. Alternatively, run the script created by
\code{translate_to_r_script()}.}

\item{add_r_command_colum}{logical, whether to add a column \code{"R command"}
specifying the corresponding R command; defaults to FALSE}

\item{rec_fun}{function either purrr::reduce2 or purrr::accumulate2; see
Value section}

\item{check_id_is_unique}{logical whether to check that the specified id
variable (in sheet "configr") is unique; defaults to TRUE.}

\item{mapping_file_attrs}{Parameter list that is read in from the "configr" sheet in \code{mapping_file}.}

\item{class}{Class attribute to create a subclass.}
}
\value{
A list object of type mapping.
}
\description{
Constructor of mapping objects
}
\examples{
mapping_file <- system.file("extdata", "mapping.xlsx", package = "datenanpassr")
new_mapping(mapping_file)
}
